# NanoClaw 轻量个人助手记忆系统开发计划

## 1. 项目目标

为 NanoClaw 增加一个轻量、高可靠、低 Token 消耗的个人助手记忆系统。

目标不是构建复杂知识库，而是让 Agent：

1. 记住用户长期稳定的信息；
2. 记住当前重要项目和决定；
3. 能搜索过去发生过的事情；
4. 在合适的时候自然引用历史，提高陪伴感；
5. 避免频繁提起记忆造成打扰；
6. 保持本地优先、简单可靠。

---

# 2. 设计原则

## 2.1 简单优先

第一阶段不引入：

* 向量数据库；
* 知识图谱；
* Mem0；
* Graphiti；
* 多 Agent Memory 服务。

优先使用：

* Markdown 文件；
* JSONL；
* SQLite FTS5 搜索。

---

## 2.2 记忆和历史分离

系统分为：

```
长期记忆
    ↓
每日记录
    ↓
完整会话历史
```

不同数据承担不同职责。

---

## 2.3 不主动炫耀记忆

Agent 不应该：

> “根据我的记忆，你喜欢……”

而应该：

> “你之前好像提过更喜欢安静一点的环境，东京这种节奏确实容易让人累。”

记忆应该服务交流，而不是展示能力。

---

# 3. 系统整体架构

```
                  用户消息
                     |
                     v
             Memory Decision
                     |
       +-------------+--------------+
       |                            |
 不需要记忆                    需要记忆
       |                            |
       v                            v
  正常对话                 Memory Search
                                    |
                                    v
                              SQLite FTS5
                                    |
                                    v
                             返回相关记忆
                                    |
                                    v
                             生成回复
```

---

# 4. 数据结构设计

## 4.1 USER.md

用途：

保存用户长期稳定信息。

路径：

```
memory/USER.md
```

示例：

```
# User Profile

## Basic

- 用户名字 Shayne
- 来自中国，目前生活在日本

## Interest

- 喜欢音乐
- 会弹贝斯
- 对 AI Agent 感兴趣

## Preference

- 喜欢直接自然的交流方式
- 不喜欢闲聊时被频繁提供解决方案
- 偏好简单可靠的软件设计
```

---

### 写入规则

允许：

* 用户身份；
* 长期兴趣；
* 长期偏好；
* 沟通习惯。

禁止：

* 临时状态；
* 一次性问题；
* AI 推测；
* 角色扮演内容。

---

### 限制

最大长度：

```
3000 字符以内
```

超过限制：

需要合并或者删除低价值内容。

---

# 4.2 MEMORY.md

用途：

保存当前工作环境、项目状态、重要决定。

路径：

```
memory/MEMORY.md
```

示例：

```
# Working Memory

## NanoClaw

- 用户正在开发个人 AI Agent。
- 当前重点是优化记忆系统。
- 日程提醒使用 Apple Calendar。
- 目标是打造个人助手，而不是企业 Agent。

## Development

- Python 项目使用 uv 管理。
- 主要运行环境为 MacBook。
```

---

### 写入规则

允许：

* 项目状态；
* 技术决策；
* 当前目标；
* 开发环境。

禁止：

* 未确认建议；
* 普通知识；
* 临时讨论。

---

### 限制

最大：

```
5000 字符以内
```

---

# 4.3 Daily Memory

用途：

保存当天发生的重要事件。

路径：

```
memory/daily/YYYY-MM-DD.md
```

示例：

```
# 2026-07-26

## NanoClaw

- 重新设计了轻量 Memory 系统。
- 放弃复杂知识图谱方案。

## Personal

- 用户希望 Agent 更像长期陪伴，而不是工具。
```

---

规则：

每日记录：

* 不限制长期价值；
* 不直接进入 USER.md；
* 后续可以整理。

---

# 4.4 Session History

保持 NanoClaw 原有：

```
sessions/*.jsonl
```

保存：

* 完整聊天；
* 工具调用；
* 上下文。

用途：

作为最终事实来源。

---

# 4.5 Follow Up Memory

用途：

保存“未来聊到相关话题时，可以顺便提醒”的事项。

文件：

```
memory/followups.jsonl
```

示例：

```
{
 "topic":"EventKit",
 "content":"用户计划研究 EventKit 接入 Apple Calendar",
 "created":"2026-07-26",
 "max_remind":2
}
```

---

规则：

* 不是任务系统；
* 不替代 Calendar；
* 只用于自然聊天提醒。

---

# 5. 功能模块开发计划

---

# Phase 1：基础 Memory 文件系统

## 目标

建立基础记忆存储能力。

---

## 开发内容

实现：

### Memory Manager

负责：

* 创建目录；
* 读取文件；
* 写入文件；
* 更新文件；
* 备份。

接口：

```
memory_add()

memory_update()

memory_remove()

memory_get()
```

---

## 验证方法

手动测试：

1. 创建 USER.md；
2. 添加用户偏好；
3. 重启 Agent；
4. Agent 可以读取。

---

## 测试案例

### Case 1

输入：

```
记住，我喜欢简单可靠的软件。
```

结果：

USER.md:

```
喜欢简单可靠的软件设计。
```

---

### Case 2

输入：

```
今天东京天气不错。
```

结果：

不写入 USER.md。

---

# Phase 2：Memory 写入控制

## 目标

避免 Agent 随意污染记忆。

---

## 开发内容

禁止：

Agent 直接修改 Markdown。

改为：

Agent 输出 Memory Operation：

例如：

```
{
"type":"add",
"target":"USER",
"content":"喜欢简单的软件设计"
}
```

程序验证后执行。

---

## 验证方法

测试：

Assistant 提出：

> 推荐使用 Mem0。

用户没有确认。

结果：

不能写入。

---

测试：

用户：

> 好，那以后使用 Mem0。

结果：

允许记录。

---

# Phase 3：每日记忆系统

## 目标

保存短期历史。

---

## 开发内容

增加：

```
daily_append()
```

功能：

每天自动创建：

```
daily/date.md
```

---

## 自动整理时机

两个：

### 会话结束

总结：

* 今日重要事件；
* 项目变化；
* 用户新偏好。

### Context 压缩前

保存即将丢失的重要信息。

---

## 验证

连续聊天：

第一天：

```
研究 NanoClaw Memory
```

第二天：

Agent 可以搜索昨天记录。

---

# Phase 4：SQLite 搜索系统

## 目标

支持历史检索。

---

## 技术方案

SQLite FTS5。

索引：

```
USER.md

MEMORY.md

daily/*.md

sessions/*.jsonl
```

---

## 工具

新增：

```
memory_search()

session_search()
```

---

## 搜索策略

优先：

```
memory_search
```

失败：

```
session_search
```

---

## 验证案例

用户：

```
之前我们怎么讨论 Apple Calendar？
```

流程：

1. 搜索 Memory；
2. 找不到；
3. 搜索 Session；
4. 返回历史内容。

---

# Phase 5：陪伴式 Memory Cue

## 目标

让 Agent 自然引用过去。

---

## 开发内容

增加：

Memory Cue。

搜索结果：

不直接进入回答。

转换：

```
相关历史：

用户喜欢安静环境。

使用建议：

如果当前话题相关，可以自然提起。
```

---

## 使用规则

限制：

### 单次最多：

1 条记忆。

### 冷却：

同一记忆：

14 天内不要主动再次提起。

### 禁止：

为了展示记忆强行引用。

---

## 测试案例

用户：

```
东京最近好烦，人好多。
```

搜索：

```
喜欢安静环境
```

允许：

```
东京这种节奏确实容易累，你之前也说过更喜欢安静一点的生活环境。
```

禁止：

```
根据我的记忆，你喜欢安静。
```

---

# Phase 6：Follow Up 系统

## 目标

提供轻量跟进能力。

---

## 开发内容

新增：

```
followup_add()

followup_complete()
```

---

## 规则

触发条件：

用户再次聊相关主题。

例如：

之前：

```
以后研究 EventKit
```

后来：

```
苹果日历怎么接？
```

可以提醒：

```
你之前提过 EventKit，后来研究了吗？
```

---

限制：

* 最多提醒 2 次；
* 用户关闭立即停止；
* 超时自动失效。

---

# 6. 关键测试场景

## 测试 1：长期偏好

输入：

```
我喜欢简单的软件。
```

预期：

写入 USER。

---

## 测试 2：临时信息

输入：

```
今天想吃火锅。
```

预期：

不写 USER。

---

## 测试 3：错误记忆修改

已有：

```
喜欢复杂架构。
```

用户：

```
其实我喜欢简单方案。
```

预期：

替换旧记录。

---

## 测试 4：主动关联

历史：

```
喜欢安静环境。
```

当前：

```
东京太吵了。
```

预期：

自然关联。

---

## 测试 5：避免骚扰

连续聊天：

用户：

```
东京很累。
```

第一次：

允许引用。

之后：

禁止连续重复。

---

# 7. 后续可选升级

只有出现真实需求再增加：

## Embedding Search

解决：

关键词搜索不到同义表达。

例如：

```
安静
```

匹配：

```
喜欢清净
```

---

## Mem0

用于：

自动记忆提取。

但需要严格过滤。

---

## Knowledge Graph

用于：

复杂人物关系。

当前个人助手不需要。

---

# 8. 最终验收标准

完成后 NanoClaw 应满足：

✅ 能记住用户长期信息
✅ 能保存项目状态
✅ 能搜索过去聊天
✅ 能自然引用历史
✅ 不频繁提醒
✅ 不污染角色扮演
✅ Token 消耗低
✅ 所有数据本地可查看
✅ 用户可以手动修改记忆文件

---

# 开发优先级总结

```
P0
|
|-- Memory 文件系统
|-- USER.md
|-- MEMORY.md
|-- 基础读写

P1
|
|-- Daily Memory
|-- SQLite 搜索

P2
|
|-- Memory Cue
|-- 陪伴式引用

P3
|
|-- Embedding
|-- 高级记忆能力
```

---

这版我认为更符合 NanoClaw 的定位：**不是打造一个企业级 Agent Memory，而是打造一个长期陪伴你的个人 AI。** 复杂度控制在一个人可以维护的范围内，同时未来也留好了升级空间。
