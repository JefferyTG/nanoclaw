# NanoClaw 图像识别（视觉能力）功能方案设计

> **文档状态**：待审阅 · 未编写任何代码
> **编写日期**：2026-07-26
> **编写者**：Senior Developer
> **审阅目的**：在动手实现前，先就「视觉/识图能力如何接入 NanoClaw」达成方案共识

---

## 摘要

NanoClaw 当前是一个**纯文本**的 ReAct Agent 网关：飞书渠道会直接丢弃图片消息（`message_type != "text"` 即 `return`），网页渠道 WebSocket 只处理 `TEXT` 帧，`AgentLoop.run()` 只接收字符串。但好消息是——**底层 Provider（OpenAI 兼容 + 硅基流动）已原生支持多模态**，只要把 messages 的 `content` 从字符串改成数组格式即可，Provider 层几乎零改动。

本文基于对硅基流动多模态 API、飞书图片下载、LobeChat / Open WebUI / Cherry Studio 等同类产品、LangChain 多模态 Agent 实践、Ollama 本地方案的调研，结合 NanoClaw 现有架构，给出 **3 个可选实现方案**，并在末尾给出推荐意见。

---

## 一、项目现状分析

### 1.1 架构与技术栈

NanoClaw 是「本地优先、多渠道、可扩展」的个人 AI Agent 网关，核心架构：

```
渠道(飞书WS / 网页WS / CLI) ──▶ MessageBus ──▶ Gateway ──▶ AgentLoop(ReAct)
                                                          ├──▶ ContextBuilder(拼 System Prompt)
                                                          ├──▶ Provider(OpenAI 兼容)
                                                          ├──▶ Tools(内置 + MCP)
                                                          └──▶ Memory(压缩/检索)
```

**技术栈**：Python 3.13+ / asyncio / OpenAI SDK / lark-oapi(飞书) / aiohttp(网页) / MCP。依赖中**无任何图像处理库**（无 PIL / opencv）。

### 1.2 现有视觉能力：**无**

逐层核对，当前对图片是「全链路不支持」：

| 层 | 文件 | 现状 | 是否阻断图片 |
|---|---|---|---|
| 飞书渠道 | `channels/feishu.py` L104 | `if message.message_type != "text": return` | ✅ 直接丢弃图片消息 |
| 网页渠道 | `channels/web.py` L216 | `if msg.type != WSMsgType.TEXT: continue` | ✅ 无图片上传通道 |
| 消息结构 | `bus/queue.py` `InboundMessage` | `content: str`（纯字符串） | ✅ 无法承载图片 |
| Agent 循环 | `agent/loop.py` L121 | `run(user_message: str)` | ✅ 只收字符串 |
| 上下文构建 | `agent/context.py` L168 | `{"role":"user","content": current_message}` | ✅ content 是纯文本 |
| 持久化 | `session/manager.py` | JSONL 存 `content` 字符串 | ✅ 无法存多模态 |
| Provider | `providers/openai_compat.py` | `AsyncOpenAI` 直接传 messages | ❌ **天然兼容多模态** |

**关键发现**：Provider 层用 `AsyncOpenAI` 的 `chat.completions.create(**request_kwargs)`，messages 原样透传。OpenAI 兼容协议本就支持 `content` 为数组（`[{"type":"text",...},{"type":"image_url",...}]`），所以**只要上游把图片塞进 messages，Provider 一行都不用改**。

### 1.3 改造点定位（从下到上 5 层）

要让 NanoClaw「看懂图」，需要改造的最小链路：

1. **模型层**：选一个支持视觉的模型（如硅基流动的 `Qwen/Qwen2.5-VL-72B-Instruct`、`Pro/Qwen/Qwen2.5-VL-...`、GLM-4V 等）。
2. **消息构建层**：`ContextBuilder.build_messages()` 的 `current_message` 支持多模态 content（数组）。
3. **循环层**：`AgentLoop.run()` 入参从 `str` 扩展为可携带图片的结构；持久化兼容多模态消息。
4. **渠道层**：飞书接收 `image` 消息并下载图片；网页增加图片上传（WS 二进制帧或 HTTP 上传）。
5. **入站消息结构**：`InboundMessage` 增加 `images` 字段（图片路径/base64/URL 列表）。

> 改造点 1~3 是「核心管线」，4~5 是「渠道适配」。不同方案对这 5 层的改动量差异很大。

---

## 二、竞品调研

### 2.1 云端多模态大模型 API（硅基流动为主，本项目当前 Provider）

**这是最契合 NanoClaw 现状的方向**，因为项目已经在用硅基流动的 OpenAI 兼容端点。

**核心能力**（来自硅基流动官方文档 `docs.siliconflow.cn/capabilities/vision`）：

- **接口完全复用** `/chat/completions`，无需换 SDK、无需换 client。
- **消息格式标准化**：
  ```json
  {
    "role": "user",
    "content": [
      {"type": "image_url", "image_url": {"url": "https://...或 data:image/jpeg;base64,...", "detail": "high"}},
      {"type": "text", "text": "描述这张图"}
    ]
  }
  ```
- **支持模型**：Qwen3-VL / Qwen2.5-VL / Qwen2-VL 系列、GLM 系列、DeepseekVL2、Step3、DeepSeek-OCR（还支持 PDF）。
- **图片输入**：URL 或 base64 data URI 均可；`detail` 参数（`low`/`high`/`auto`）控制 token 消耗——`low` 统一缩到 448×448 约 256 tokens，`high` 按实际像素计费。
- **计费**：图片按 token 计费，单张 `low` 约 256 tokens，`high` 视分辨率从 128 到上万 tokens 不等。

**对 NanoClaw 的意义**：Provider 层（`OpenAICompatProvider`）**一行都不用改**。只要把 messages 的 content 从字符串换成数组，AsyncOpenAI 会原样发出。模型名换成视觉模型即可。

### 2.2 飞书机器人接收/下载图片

**接收**：消息事件里 `message.message_type == "image"`，`message.content` 是 `{"image_key": "img_v2_xxx"}`。当前代码 L104 的 `!= "text"` 判断把这类消息直接丢弃。

**下载**（关键坑点，来自飞书文档与社区踩坑记录）：
- 正确接口：`GET /open-apis/im/v1/messages/{message_id}/resources/{file_key}?type=image`
- **必须带 `message_id`**，单独用 `image_key` 调 `/im/v1/images/{image_key}` 会返回 400（那个接口只用于下载机器人自己上传的图片）。
- 返回二进制流，限频 1000 次/分钟、50 次/秒，单文件 ≤100MB。
- lark-oapi SDK 提供 `client.im.v1.message_resource.get` 方法封装此接口。

**实现要点**：在 `_on_message` 回调里，把 `image` 类型消息的 `image_key` + `message_id` 取出，异步下载二进制 → base64 编码 → 塞进 `InboundMessage`。注意飞书回调有 3 秒超时，下载需放到 Gateway 侧异步做（与现有「只投总线、立即返回」模式一致）。

### 2.3 同类个人 AI 助手的视觉实现

| 产品 | 视觉实现方式 | 与 NanoClaw 的关系 |
|---|---|---|
| **LobeChat** | 前端图片 → Base64/URL → OpenAI 兼容 `image_url` 字段；原生多模态，支持 TTS | 纯前端方案，前端 Base64 编码后走标准接口，**做法可直接借鉴** |
| **Open WebUI** | 支持图片上传；可通过 OpenAI 兼容端点接 Gemini/GLM-4V；深度集成 Ollama 本地视觉模型 | 架构上最接近 NanoClaw（自托管、本地优先） |
| **Cherry Studio** | 支持文本/图片/Office/PDF；50+ 服务商；本地优先 + MCP 支持 | **理念最接近 NanoClaw**（本地优先、MCP 扩展），它的多模态接入路径值得参照 |
| **Chatbox** | 不支持视觉模型（短板） | 反面案例 |

**通用模式**：前端把图片 Base64 编码 → 通过 OpenAI 兼容接口的 `image_url` 字段发送 → 后端原样转发给视觉模型。几乎所有现代多模态客户端都走这条路。

### 2.4 Agent 框架的多模态实践（LangChain / LangGraph）

LangChain 生态有两种主流多模态 Agent 模式：

1. **视觉模型直通**：把图片直接放进 messages 传给支持视觉的 LLM，Agent 的推理循环不变。简单直接，适合「用户发图 + 提问」场景。
2. **视觉即工具（Vision as Tool）**：把视觉模型封装成一个 Tool（如 `analyze_image(image_path, question)`），让 ReAct Agent 在需要时主动调用。适合「图片是任务的一部分，但主模型不一定是视觉模型」的场景。

**关键工程经验**（来自社区实践）：
- 图片消耗 token 量大（85~765 tokens/张），**必须控制图片数量与尺寸**，建议上传前 resize 到 1024×1024、JPEG 质量 85。
- 不要用文本摘要代替原图传给 VLM——会丢失视觉细节。
- 多模态 RAG 要用独立的图像检索器，不能与文本检索混用。

**对 NanoClaw 的意义**：本项目本来就是 ReAct + Tool 架构，**「视觉即工具」模式天然契合**，且能复用现有 Tool 基类（`agent/tools/base.py`）。

### 2.5 本地多模态方案（Ollama）

- **调用方式**：`ollama.chat(model, messages, images=[base64_str])` —— `images` 字段传 base64 列表。
- **可用模型**：`llama3.2-vision`、`qwen2-vl:7b`、`glm4v:9b`（INT4 量化约 9GB，单 RTX 4090 可跑）。
- **优势**：完全本地、无 API 费用、数据不出设备（与 NanoClaw「本地优先」理念一致）。
- **劣势**：必须有 GPU（至少 8GB 显存跑 7B 量化版）；推理速度远慢于云端；中文 OCR 能力弱于 Qwen-VL-72B 等大参数云端模型。

### 调研小结

| 维度 | 云端 VLM API | 飞书/网页渠道 | Agent 框架 | 本地 Ollama |
|---|---|---|---|---|
| 与现状契合度 | ⭐⭐⭐⭐⭐（Provider 零改动） | ⭐⭐⭐（需改渠道层） | ⭐⭐⭐⭐（Tool 机制现成） | ⭐⭐（需额外推理后端） |
| 实现成本 | 低 | 中 | 低~中 | 高 |
| 能力上限 | 高（72B 模型） | — | — | 中（7~9B 量化） |
| 隐私 | 数据上云 | — | — | 全本地 |

---

## 三、可借鉴性分析（结合 NanoClaw 技术栈）

### 3.1 云端 VLM（硅基流动）—— 可借鉴性：极高 ✅

**可行性**：
- Provider 层（`OpenAICompatProvider`）已用 AsyncOpenAI，messages 原样透传，**零改动**。
- 只需在 `config.json` 把 `model` 换成视觉模型（如 `Qwen/Qwen2.5-VL-72B-Instruct`），或在配置里新增 `vision_model` 字段按需切换。
- 硅基流动是当前默认 `base_url`，无需额外申请服务。

**潜在风险**：
- **视觉模型与现有文本模型差异**：当前默认 `Pro/moonshotai/Kimi-K2.5` 是纯文本模型，视觉模型（Qwen-VL 等）在纯文本推理/工具调用上可能不如 Kimi-K2.5。若全局切换可能影响现有体验 → 建议**按需切换**（有图用视觉模型，无图用文本模型）或在配置里分开。
- **成本**：图片按 token 计费，`detail=high` 的大图单张可达上万 tokens，需加图片尺寸/数量上限保护。
- **流式兼容**：视觉模型的流式输出硅基流动已支持（`stream=True`），现有 `chat_stream` 路径可复用，但需实测验证图片+流式组合。

### 3.2 飞书图片接入 —— 可借鉴性：高 ✅，但有坑

**可行性**：
- lark-oapi 已是依赖，`client.im.v1.message_resource.get` 可直接下载图片二进制。
- 改造点集中在 `FeishuChannel._on_message`：新增 `image` / `post`(富文本含图) 类型分支。

**潜在风险**：
- **下载接口的 message_id 依赖**：必须用 `messages/{message_id}/resources/{file_key}` 而非 `/images/{image_key}`，否则 400（这是文档里没写清、社区踩坑最多的点）。
- **3 秒回调超时**：飞书事件回调限时 3 秒，图片下载不能在回调里做，必须「取 key → 投总线 → Gateway 侧异步下载」，与现有「只投总线、立即返回」模式一致，但要扩展 `InboundMessage` 携带图片下载所需信息。
- **图片体积**：飞书原图可能很大（几 MB），直接 base64 会让消息体积膨胀 ~33%，建议下载后先 resize 再编码。

### 3.3 网页渠道图片上传 —— 可借鉴性：高 ✅

**可行性**：
- 网页前端加 `<input type="file" accept="image/*">`，`FileReader.readAsDataURL` 转 base64。
- 通过 WS 发送 JSON（`{"type":"image","data":"data:image/jpeg;base64,..."}` 或与文本合并）。
- 后端 `_handle_ws` 增加二进制/JSON 图片分支。

**潜在风险**：
- **WS 单帧大小**：aiohttp WS 默认有最大消息限制（默认 4MB），大图 base64 可能超限 → 需调大 `max_msg_size` 或前端压缩。
- **前端改造量**：`webui/index.html` 是单页前端，需加图片预览、上传交互，工作量不小但独立于后端。

### 3.4 视觉即工具（Vision as Tool）—— 可借鉴性：高 ✅

**可行性**：
- 本项目 `Tool` 基类（`agent/tools/base.py`）成熟，新增一个 `AnalyzeImageTool` 完全契合现有 `to_function_definition` 机制。
- 模型可自主决定何时调视觉工具，符合 ReAct 哲学。

**潜在风险**：
- **主模型需认识图片路径**：工具模式要求图片先落到本地文件（或 URL），主模型（纯文本）拿到的是路径/URL 字符串，**无法直接「看到」图**。这意味着用户发图时，仍需先把图塞进主对话的多模态 content，工具模式更适合「Agent 主动去分析工作区里某个图片文件」的内部场景。
- **结论**：工具模式不能替代「用户直接发图」的多模态对话，更适合作为补充能力（如 Agent 读图后做后续处理）。

### 3.5 本地 Ollama 视觉 —— 可借鉴性：中 ⚠️

**可行性**：
- 理念契合 NanoClaw「本地优先」，但需要额外跑一个 Ollama 服务（新依赖、新进程）。
- 调用方式与 OpenAI 兼容接口不同（`images` 字段 vs `image_url` content），需另写一个 Provider 或用 Ollama 的 OpenAI 兼容端点（`/v1/chat/completions`，但多模态支持参差）。

**潜在风险**：
- **硬件门槛**：Mac 需 M 系列芯片 + 足够统一内存，或独立 NVIDIA GPU；NanoClaw 目标是「单台设备跑多实例」，叠加本地视觉模型会显著抬高资源门槛。
- **能力差距**：本地 7~9B 量化模型在中文 OCR、复杂图表理解上明显弱于云端 72B 模型。
- **维护成本**：多一套推理后端要维护。

---

## 四、实现方案（3 个可选）

> 三个方案并非互斥，可组合。下文先各自独立评估，再给对比表。

### 方案 A：云端 VLM 直通（多模态对话）⭐ 推荐

**核心思路**：让用户能直接在对话里发图，图片随消息一起进入 Agent 主循环，由云端视觉模型（硅基流动 Qwen-VL / GLM-4V 等）直接理解。主模型本身就是视觉模型，ReAct 循环不变。

**技术选型方向**：
- 模型：`Qwen/Qwen2.5-VL-72B-Instruct`（主推，中文强）或 GLM-4V 系列；建议在 `config.json` 新增 `vision_model` 字段，有图时临时切换模型，无图沿用 `model`。
- 消息构建：`ContextBuilder.build_messages()` 的 `current_message` 从 `str` 扩展为 `str | list`（OpenAI 多模态 content 数组）；持久化兼容数组型 content。
- 渠道：
  - 飞书：`_on_message` 增加 `image` 分支，异步下载图片 → resize → base64，塞进 `InboundMessage.images`。
  - 网页：前端加图片上传，WS 发 JSON 携带 base64；后端 `_handle_ws` 解析。
- 入站消息：`InboundMessage` 增加 `images: list[str]`（base64 data URI 列表，可选）。
- 循环层：`AgentLoop.run()` 入参扩展为 `run(user_message, images=None)`；构建 messages 时把图片拼到用户消息 content 数组里。
- Provider 层：**不改**。

**优点**：
- ✅ 体验最好——用户发图 + 提问，模型直接看图回答，自然顺畅。
- ✅ Provider 零改动，改造集中在消息构建 + 渠道层。
- ✅ 视觉模型能力强（72B 中文 OCR / 图表理解优秀）。
- ✅ 与现有 ReAct 循环、工具调用、流式输出全部兼容。

**缺点**：
- ❌ 成本：图片按 token 计费，需加图片数量/尺寸上限。
- ❌ 全局切视觉模型可能影响纯文本场景的推理/工具调用质量 → 需「按需切换模型」逻辑（有图切 vision_model，无图用 model）。
- ❌ 数据上云（与「本地优先」理念有张力，但图片理解本身强依赖大模型，本地小模型能力有限）。
- ❌ 持久化要改：JSONL 存 base64 图片会膨胀，建议存图片路径或图片消息单独标记（重放时若文件已删则降级为占位）。

**工作量估计**：中等。核心管线（消息构建 + 循环 + 持久化）1~2 天，飞书渠道 1 天，网页前端 1~2 天。

---

### 方案 B：视觉即工具（Vision as Tool）

**核心思路**：不改动主对话管线的多模态支持，而是新增一个 `analyze_image` 工具。Agent 在需要时调用该工具，工具内部调用视觉模型分析指定图片（本地路径/URL），返回文本描述给主模型。主模型保持纯文本（如 Kimi-K2.5）。

**技术选型方向**：
- 新增 `agent/tools/vision.py` 的 `AnalyzeImageTool`：
  - 参数：`image_path`（工作区内图片）或 `image_url`，`question`（提问）。
  - 内部用独立的视觉 Provider（可复用 `OpenAICompatProvider` + 视觉模型名）调用 VLM，返回文本。
- 注册到 `ToolRegistry`，模型按需调用。
- 主对话管线**完全不改**，`InboundMessage` / `AgentLoop.run` / 持久化都不动。

**优点**：
- ✅ 改动最小——只加一个工具文件 + 注册一行，主管线零侵入。
- ✅ 主模型不变，纯文本场景体验不受影响。
- ✅ 符合「大道至简」原则（不引入与现有机制重复的抽象）。
- ✅ 适合 Agent 自主分析工作区内图片文件的场景（如「看看 workspace/screenshot.png 里报了什么错」）。

**缺点**：
- ❌ **用户无法直接在飞书/网页发图**——因为主对话不支持多模态，用户发的图得先存成文件路径才能被工具读取。这削弱了「随手拍/截图提问」的核心体验。
- ❌ 多一跳：用户图 → 存文件 → 模型决定调工具 → 工具调 VLM → 返回描述 → 模型综合回答。延迟更高，且模型可能不调工具。
- ❌ 工具返回的是文本描述，主模型「隔层纱」看图，细节会丢失。

**适用场景**：作为方案 A 的补充，让 Agent 能主动分析工作区内的图片文件；或作为「最小成本先跑起来」的过渡方案。

**工作量估计**：小。1 个工具文件 + 注册，半天到 1 天。

---

### 方案 C：本地多模态（Ollama 视觉模型）

**核心思路**：本地跑 Ollama 视觉模型（qwen2-vl / glm4v / llama3.2-vision），NanoClaw 通过 Ollama 的接口调用，实现完全离线的图片理解。管线改造同方案 A（多模态对话直通），只是把云端 VLM 换成本地 Ollama。

**技术选型方向**：
- 推理后端：Ollama（需用户另装 + 拉视觉模型，如 `ollama pull qwen2vl:7b`）。
- Provider：新增 `OllamaVisionProvider`，用 `ollama` Python 库或其 OpenAI 兼容端点（`http://localhost:11434/v1`，但多模态支持需验证）。
- 其余改造同方案 A（消息构建、渠道、循环、持久化）。

**优点**：
- ✅ 完全本地，数据不出设备，与「本地优先」理念最契合。
- ✅ 无 API 费用，图片再多也不心疼。
- ✅ 离线可用。

**缺点**：
- ❌ **硬件门槛高**：需 GPU（≥8GB 显存跑 7B 量化）或 M 系列高内存 Mac；与「单台设备跑多实例」的资源目标冲突。
- ❌ **能力差距明显**：本地 7~9B 模型在中文 OCR、复杂图表、多图对比上远弱于云端 72B。
- ❌ 推理慢（本地几秒~十几秒一张图），流式体验差。
- ❌ 多一套推理后端要维护，部署复杂度上升。
- ❌ 与方案 A 的渠道/管线改造量相同，但收益（能力）更低。

**适用场景**：对隐私极度敏感、且硬件充裕的用户；或作为方案 A 的可选后端（配置里切换 Provider）。

**工作量估计**：大。方案 A 的全部工作 + Ollama 集成 + 新 Provider，3~5 天（不含用户侧 Ollama 部署）。

---

### 方案对比总表

| 维度 | 方案 A：云端 VLM 直通 | 方案 B：视觉即工具 | 方案 C：本地 Ollama |
|---|---|---|---|
| **用户体验** | ⭐⭐⭐⭐⭐ 直接发图提问 | ⭐⭐ 需先存文件，模型隔层纱 | ⭐⭐⭐⭐ 直接发图（但慢） |
| **实现成本** | 中（管线+渠道） | 小（1 个工具） | 大（管线+渠道+Ollama） |
| **改动侵入性** | 中（5 层中改 4 层，Provider 不动） | 极小（只加工具） | 中高（同 A + 新 Provider） |
| **视觉能力** | 强（72B 云端） | 强（工具内可调云端 VLM） | 中（7~9B 本地） |
| **隐私** | 数据上云 | 数据上云（工具调用） | 全本地 |
| **成本** | 按量计费 | 按量计费 | 免费（硬件成本） |
| **与现状契合** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐ |
| **能否直接发图** | ✅ | ❌（需存文件路径） | ✅ |

---

## 五、推荐与下一步

### 5.1 推荐意见

**主推方案 A（云端 VLM 直通）+ 可选叠加方案 B（视觉即工具）**：

1. **方案 A 作为主路径**：它能让用户在飞书/网页直接发图提问，这是「图像识别功能」最符合直觉的体验，且 Provider 零改动、与现有 ReAct/流式/工具调用全兼容。
2. **方案 B 作为补充**：让 Agent 能主动分析工作区内已存在的图片文件（如截图、日志图），与方案 A 共存，成本极低。
3. **方案 C 暂不推荐**：硬件门槛与能力差距使其性价比不高，可留作未来「隐私优先模式」的可选项（做成 Provider 可切换即可，届时方案 A 的管线改造可直接复用）。

**模型选型建议**：`Qwen/Qwen2.5-VL-72B-Instruct`（硅基流动，中文 OCR/图表强）作为默认视觉模型；在 `config.json` 新增 `vision_model` 字段，有图时切换、无图沿用 `model`，避免视觉模型影响纯文本场景。

### 5.2 若采纳方案 A，下一步待确认事项

在动手编码前，建议先就以下几点达成共识：

1. **模型切换策略**：是「有图自动切 vision_model」还是「全局用 vision_model」？前者更省、后者更简单。
2. **图片上限策略**：单轮图片数量上限（建议 ≤4）、单张尺寸上限（建议长边 ≤1280px，上传前 resize）、`detail` 默认值（建议 `auto`）。
3. **持久化策略**：带图消息在 JSONL 里存 base64（简单但文件大）还是存图片文件路径（轻量但重放需文件存在）？
4. **飞书 vs 网页优先级**：先做哪个渠道？网页前端改造较独立，飞书需处理图片下载的 message_id 坑。
5. **是否同时落地方案 B**：作为 Agent 自主分析工作区图片的补充工具，是否一并实现。

### 5.3 风险提示

- 飞书图片下载的 `message_id` 依赖是高频踩坑点，实现时务必用 `messages/{message_id}/resources/{file_key}` 接口。
- 视觉模型的 `tool_calls` 行为可能与 Kimi-K2.5 有差异（部分 VLM 对 function calling 支持较弱），需实测 Qwen-VL 在带工具场景下的表现。
- 网页 WS 单帧大小默认 4MB，大图 base64 易超限，需调 `max_msg_size` 或前端压缩。

---

## 附录 A：改造点清单（方案 A）

| # | 文件 | 改动 |
|---|---|---|
| 1 | `config.py` / `config.json` | 新增 `vision_model` 字段（可选） |
| 2 | `bus/queue.py` | `InboundMessage` 增加 `images: list[str] = None` 字段 |
| 3 | `agent/context.py` | `build_messages` 的 `current_message` 支持 `str \| list`，按需拼多模态 content |
| 4 | `agent/loop.py` | `run(user_message, images=None)`；构建 messages 时拼图片；模型按需切换 |
| 5 | `channels/feishu.py` | `_on_message` 增加 `image` 分支，异步下载图片→base64 |
| 6 | `channels/web.py` | `_handle_ws` 增加图片 JSON 解析；`max_msg_size` 调大 |
| 7 | `webui/index.html` | 加图片上传 UI + base64 编码 + WS 发送 |
| 8 | `session/manager.py` | 持久化兼容数组型 content（验证现有 JSONL 读写是否需调整） |
| 9 | `providers/openai_compat.py` | **不改**（已兼容） |

## 附录 B：参考文档

- 硅基流动多模态：`https://docs.siliconflow.cn/capabilities/vision`
- 飞书获取消息资源：`https://open.feishu.cn/document/uAjLw4CM/ukTMukTMukTM/reference/im-v1/message-resource/get`
- OpenAI 多模态消息格式：`https://platform.openai.com/docs/guides/vision`
- LangChain 多模态 Agent 实践（社区）

---

> **请审阅后告知：** ①方案选择（A / A+B / 其他）②上述 5 个待确认事项的决策 ③是否开始进入实现阶段。在你确认前，我不会编写任何代码。
