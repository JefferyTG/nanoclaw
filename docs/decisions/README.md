# 决策记录（ADR 风格，可选）

本目录存放**单条**架构/技术决策记录（ADR 风格），命名规范：`NNNN-简短标题.md`（编号递增）。

- 历史决策**总表**、约束与遗留问题统一维护在 `../DECISIONS.md`，以那里为准。
- 本目录仅用于记录需要展开论述的单个重大决策（背景、方案对比、权衡、结论、影响）。
- 简单决策直接更新 `DECISIONS.md` 即可，不必在此建文件。

模板参考 `../../docs/decisions/`（如无模板可参照 `docs/DEVELOPMENT.md` 交接格式）。
