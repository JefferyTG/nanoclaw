# NanoClaw 协作规则

本文件适用于整个仓库。所有开发会话和子 Agent 开始工作前必须先读本文件，以及与任务相关的 `docs/` 文档。

## 事实源与入口

事实冲突时按以下顺序判断：当前代码与配置契约 > 当前 Git 状态 > `docs/` 中已确认文档 > `.workbuddy/` 历史日志 > 旧方案草稿。历史日志说明“当时为什么这样做”，不能替代当前代码核验。

开始任务前至少检查：

```bash
git status --short --branch
git log -1 --oneline
```

项目入口和长期文档：

- `PROJECT.md`：项目总览入口（定位、能力、模块、命令速查）。
- `README.md`：安装、配置和使用方法。
- `docs/ARCHITECTURE.md`：当前架构、模块边界和数据流。
- `docs/DECISIONS.md`：历史决策、已知限制和遗留问题。
- `docs/DEVELOPMENT.md`：任务拆解、跨会话协作、验证和完成标准。
- `docs/tasks/TEMPLATE.md`：长任务的任务卡与交接模板。

## 环境与依赖

- Python 环境统一用 `uv`；使用 `uv sync`、`uv add`、`uv remove`、`uv run ...`。
- 不用全局 Python 或 `pip install` 修改项目环境。
- Python 最低版本为 3.13，依赖声明在 `pyproject.toml`，解析结果锁在 `uv.lock`。
- 项目按 `uv run python main.py` 直接运行，沿用现有顶层导入方式；不要在局部改成 `nanoclaw.*` 包导入。

## 修改边界

- 只改任务授权范围内的文件；发现额外问题先记录，不顺手扩大实现范围。
- 不覆盖、回滚或提交其他会话/用户已有的未提交修改。若范围重叠，先停止写入并报告项目负责人。
- `config.json`、`.workbuddy/`、`workspace/`、`sessions/`、`identity*.md`、日志、图片、`deploy/`、`scripts/` 和本地虚拟环境属于敏感数据、运行产物或本机配置，默认不读取内容、不提交、不改动。
- 未经明确授权，不 push、不部署、不改运行中的实例、不调用真实付费模型或外部副作用接口。
- 同一文件同一时间只能有一个写入负责人。跨模块协议变更由一个集成负责人统一落地共享入口。

## 设计约定

- 优先复用现有能力，避免增加与已有工具重复的抽象。
- 新增或修改工具前先查同类实现，保持参数命名、路径校验、列表归一和构造方式一致。工作区本地文件参数统一用 `file_path`。
- `SKILL.md` 只描述行为机制，不写死人设措辞；语气由 `identity.md` 决定。
- 需要稳定执行的核心行为规则进入 System Prompt；Skill 适合按需操作指南。
- 默认产品形态是“一台设备运行多个相互独立的进程实例”，每个实例使用独立配置、workspace、端口和飞书应用；不要擅自引入进程内多 Agent 平台。
- 当前所谓 workspace 边界不是操作系统沙箱。涉及文件、Shell、网络或 Web 管理面时，必须按安全边界重新评估，不能仅依赖现有注释。

## 验证与交接

- 所有改动至少运行 `git diff --check`。
- Python 改动至少编译相关文件并执行 `uv run python -c "import main"`。
- Bug 修复应留下可重复的回归测试；不要再把唯一的验证脚本在完成后删除。
- 配置、消息协议、Web、MCP、图片或持久化改动必须按 `docs/DEVELOPMENT.md` 的验证矩阵检查关联层。
- 交接必须报告：状态、改动文件、关键决策、实际验证命令与结果、未验证项、风险、当前 Git 状态。不得只说“已完成”。
- 只有项目负责人可以最终确认完成、更新中央任务状态或合并跨会话结果。
