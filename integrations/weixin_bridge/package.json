{
  "name": "nanoclaw-weixin-bridge",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {"node": ">=20"},
  "scripts": {"test": "node --test", "build": "node --check bridge.mjs && node --check lib/*.mjs"},
  "dependencies": {
    "qrcode-terminal": "0.12.0",
    "wechat-ilink-client": "0.1.0"
  }
}
